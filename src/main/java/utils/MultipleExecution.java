package utils;

public class MultipleExecution {

    public MultipleExecution(int[][]configurations, int[]repetitions, int executionTime, double sifnificance) {
        Statistics[]statistics = new Statistics[repetitions.length];

        for (int i = 0; i < repetitions.length; i++) {
            for (int j = 0; j < repetitions[i]; j++) {
                
            }
        }
    }
}
